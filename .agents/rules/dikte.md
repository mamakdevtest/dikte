# Dikte workspace rules (Antigravity)

Always read and follow `AGENTS.md` at the repository root before working;
it points to the canonical knowledge layer under `ai/`. These rules add no
project knowledge — they only route you to the canonical files.
